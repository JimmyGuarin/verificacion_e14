# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
### NoDocs ###
GET     /                           controllers.HomeController.reactApp

### NoDocs ###
#GET   /docs/swagger-ui/*file        controllers.Assets.at(path:String="/public/lib/swagger-ui", file:String)

# Map static resources from the /public folder to the /assets URL path
### NoDocs ###
#GET     /static/*file               controllers.Assets.versioned(path="/public", file: Asset)
#GET     /static/*file               controllers.HomeController.assetOrDefault(file)

# Api definition
->     /api                          api.Routes

### NoDocs ###
GET     /*file                      controllers.HomeController.assetOrDefault(file)